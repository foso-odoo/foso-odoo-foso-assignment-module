<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_stock_picking_batch_graph" model="ir.ui.view">
        <field name="name">stock.picking.batch.graph</field>
        <field name="model">stock.picking.batch</field>
        <field name="arch" type="xml">
            <graph string="Inventory Batch Graph" type="bar">
                <field name="scheduled_date" type="row" interval="day"/>
                <field name="category_id" type="col"/>
                <!-- <field name="id" type="measure" operator="count" string="Count"/> -->
                <field name="transfers" type="measure" operator="sum" string="Transfers"/>
            </graph>
        </field>
    </record>

    <!-- <record id="view_stock_picking_batch_gantt" model="ir.ui.view">
    <field name="name">stock.picking.batch.gantt</field>
    <field name="model">stock.picking.batch</field>
    <field name="arch" type="xml">
        <gantt date_start="scheduled_date" date_stop="scheduled_date" default_group_by="doc_id,category_id">
            <field name="name" string="Name"/>
            <field name="total_weight" string="Weight"/>
            <field name="total_volume"  string="Volume"/>
        </gantt>
    </field>
</record> -->

 <record id="view_stock_picking_batch_gantt" model="ir.ui.view">
        <field name="name">stock.picking.batch.gantt.view</field>
        <field name="model">stock.picking.batch</field>
        <field name="arch" type="xml">
            <gantt 
                date_start="scheduled_date" 
                date_stop="scheduled_date" 
                default_group_by='doc_id,vehicle_id'
                default_scale="week"
                scales="day,week,month,year"
                precision="{'day': 'hour:full', 'week': 'day:full', 'month': 'day:full', 'year': 'day:full'}"
                decoration-info="state == 'draft'"
                decoration-success="state == 'done'"
                color="doc_id"
            >
            <field name='state' invisible='1'/>
            <field name="name" />
            <field name="total_weight" />
            <field name="total_volume" />
            </gantt>
        </field>
    </record>



   <record id="stock_picking_batch.stock_picking_batch_action" model="ir.actions.act_window">
        <field name="name">Inventory Batch</field>
        <field name="res_model">stock.picking.batch</field>
        <field name="view_mode">tree,form,kanban,graph,gantt</field>
    </record>

   
</odoo>
